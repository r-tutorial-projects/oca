class CircleArea {
	public static void main(String... args) {
		double pi = 3.1416;
		double r = 1;
		double area = pi * r * r;
		System.out.println("area of circle with ø of " + (r * 2) + " is: " + area);
	}
}
